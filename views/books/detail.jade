extends ../layout
block content
    .container
        br
        .row 
            .col-md-4
                img(src="#{book.coverImagePath}" style="width:100%;height:200px")
            .col-md-8
                h2 #{book.title}
                h6 Publish Date : #{moment(book.publishDate).format('DD-MM-YYYY')}
                hr
                p #{book.description}
                a.btn.btn-info.me-2(role="button" href='#') Rental